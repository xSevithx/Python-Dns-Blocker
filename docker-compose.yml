services:
  dns-blocker:
    build: .
    container_name: dns-blocker
    restart: unless-stopped
    ports:
      - "53:53/udp"
    env_file:
      - .env
    environment:
      - TZ=${TZ:-America/Chicago}
    volumes:
      - dns-logs:/app

volumes:
  dns-logs:
